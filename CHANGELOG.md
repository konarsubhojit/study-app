# Changelog

Release entries are generated from merged pull-request labels by the
[tag release workflow](.github/workflows/release.yml). Each `vMAJOR.MINOR.PATCH` GitHub Release is
the changelog entry for the artifacts attached to that exact tag; its generated Play note is
attached as `whatsnew-en-US`.
