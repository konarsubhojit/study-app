package dev.studyflow.core.ui.mvi

/** Immutable, complete data required to render a screen. */
public interface UiState

/** A user action or other input handled by a screen ViewModel. */
public interface UiEvent

/** A one-off outcome such as navigation or a snackbar. */
public interface UiEffect
